## Memorie

memoria → differenti tipi di velocità:
  - HDD (memoria secondo livello)
  - RAM (memoria centrale/primo livello)
  - Cache
  - Registri CPU
Si distinguono per la velocità di lettura/scrittura → quanto più ci si avvicina alla CPU tanto più devono avere la stessa velocità.
